# Backend

Python + Flask + Flask-SocketIO

## Prerequisites

- Python 3.10+
- [uv](https://docs.astral.sh/uv/getting-started/installation/). Can be installed using `pip install uv`

## Run locally

```bash
uv sync
uv run python src/main.py
```

